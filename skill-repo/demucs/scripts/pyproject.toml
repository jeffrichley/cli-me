[project]
name = "demucs-cli"
version = "0.1.0"
description = "Agent-native CLI for Demucs music source separation"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.15.0",
    "rich>=13.0.0",
]

[project.scripts]
demucs-cli = "demucs_cli:app"
