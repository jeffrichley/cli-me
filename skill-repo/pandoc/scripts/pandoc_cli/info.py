"""info command group — thin CLI dispatch.

Phase 3 fills in:
  @info_app.command("version") — show installed pandoc version
  @info_app.command("formats") — list input/output formats
  @info_app.command("engines") — list PDF engines available on PATH
"""

from __future__ import annotations
